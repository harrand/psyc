putchar : (c : i64) -> i64 = extern;

my_data : struct
{
	x : i64 = 21250;
}

main : () -> i64
{
	my_var : my_data;
	my_var.poggers;

	putchar('{');
	print_chars('!', '~');
	putchar('}');
	return 0;
}

print_chars : (beg : i64, end : i64) -> u0
{
	n : i64;
	for(= n beg; != n end; = n + n 1)
	{
		putchar(n);	
	}
}

== build ==
{
	= this_code does_nothing;
}