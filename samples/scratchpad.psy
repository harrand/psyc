putchar :: (ch : i8) -> u0 := extern;
print_digit :: (digit : i64) -> u0
{
	putchar((48 + digit)@i8);
}

main :: () -> i64
{
	print_digit(1);
	print_digit(2);
	print_digit(3);
	return 0;	
}

== default : build ==
{
	set_build_type(debug);
	set_linkage_type(executable);
}