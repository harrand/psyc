putchar : (c : i64) -> i64 = extern;

my_data : struct
{
	x : i64 = '[';
}

main : () -> i64
{
	my_var : my_data;
	putchar(my_var.x);

	print_chars('!', '~');

	= my_var.x ']';
	putchar(my_var.x);
	return 0;
}

print_chars : (beg : i64, end : i64) -> u0
{
	n : i64;
	for(= n beg; != n end; = n + n 1)
	{
		putchar(n);	
	}
}

== build ==
{
	= this_code does_nothing;
}