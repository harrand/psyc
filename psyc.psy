== default ==
{
	add_source_file("psyutil.psy");
};

main : func(-> v0)
{
	mystr : sstr mut;
	mystr.data = "hello";
	mystr.len = 5;

	iargc : s32 mut;
	iargv : u8?? mut;
	ienvp : u8?? mut;
	// todo: inlineasm to setup these params
	putchar('z');
	putsstr(mystr);

	exit(0);
	unreachable;
};
