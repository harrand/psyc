args_t : struct
{
	build_file_name : u8?;
};

write_arg : func(arg : u8? -> v0)
{
	linux_write(1, arg, zstrlen(arg));
};

parse_args : func(-> v0)
{
	i : s64 mut;

	ptr : u8? mut;
	for(i = 0; i < argc; i = i + 1)
	{
		ptr = [argv # i];
		linux_write(1, ptr, zstrlen(ptr));
	}
};
