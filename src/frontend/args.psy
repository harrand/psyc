args_t : struct
{
	build_file_name : u8?;
};

parse_arg : func(arg : u8?? -> v0)
{
	linux_write(1, [arg], zstrlen([arg]));
};

parse_args : func(-> v0)
{
	i : s64 mut;
	for(i = 0; i < argc; i = i + 1)
	{
		parse_arg(argv # i);
	}
};
