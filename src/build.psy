invoke_build_region ::= func(node : ast? -> v0)
{
	i : u64 mut;
	for(i = 0, i < (node->children_count), i = i + 1)
	{
		invoke_build_instruction(ast_get_child(node, i));
	}
};

invoke_build_instruction ::= func(node : ast? -> v0)
{
};
