irprog : struct
{
	funcs : irfunc mut?;
	funcs_count : u64;
	funcs_cap : u64;

	asms : irasm mut?;
	asms_count : u64;
	asms_cap : u64;

	globs : irglob mut?;
	globs_count : u64;
	globs_cap : u64;

	//entrypoint : irpos;
};
