irglob : struct
{
	idx : u64;
	type : tyid;
	init_val : valcompile_time;
};

bwrite_irglob : func(w : bwriter mut?, glob : irglob -> v0)
{
	bwrite_tyid(w, glob.type);
	bwrite_zstr(w, " @");
	bwrite_uint(w, glob.idx);
};
