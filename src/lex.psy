Sleep ::= func(millis : s32) -> v0 := extern;

lex_slice ::= struct
{
	off : u64;
	len : u64;
};

lex ::= func() -> v0
{
	psyc_timed(psyc_stage.lex);
};

== build ==
{
	add_source_file("diag.psy");
}
