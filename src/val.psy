val_tag : enum
{
	// not a valid value (no data)
	.invalid := 0;
	// a runtime scalar value (d0 = irval (d1, d2, d3 etc... are a part of the irval object and the rest of the val struct is assumed to be large enough to store the whole thing))
	.scalar := 1;
	// similar to scalar, but the irval stores a pointer to the value instead of the value directly (d0 = irval (d1, d2, d3 etc... are a part of the irval object and the rest of the val struct is assumed to be large enough to store the whole thing))
	.pointer := 2;
	// a bunch of irvals representing a complex value such as a struct value (d0 = member irvals?, d1 = count of members. remember this isnt necessarily the number of members of the struct)
	.aggregate := 3;
};

val : struct
{
	tag : val_tag;
	// datapoint 0 - use depends on tag
	d0 : u64;
	// datapoint 1 - use depends on tag
	d1 : u64;
	// datapoint 2 - use depends on tag
	d2 : u64;
	// datapoint 3 - use depends on tag
	d3 : u64;
	// datapoint 4 - use depends on tag
	d4 : u64;
	// datapoint 5 - use depends on tag
	d5 : u64;
	// type
	type : ty;
};

// note HH: sizeof(val.d1->end) must be >= sizeof(irval) otherwise this will buffer overrun
val_scalar_get_irval : func(v : val mut? -> irval mut?)
{
	return ref(v->d0)@irval mut?;
};

val_pointer_get_irval : func(v : val mut? -> irval mut?)
{
	return ref(v->d1)@irval mut?;
};

val_pointer_get_displacement : func(v : val mut? -> s32 mut?)
{
	return ref(v->d0)@s32 mut?;
};

val_aggregate_get_members : func(v : val mut? -> val mut? mut?)
{
	return ref(v->d0)@val mut? mut?;
};

val_aggregate_get_members_count : func(v : val mut? -> u64 mut?)
{
	return ref(v->d1)@u64 mut?;
};

val_aggregate_get_chunks : func(v : val mut? -> abi_chunk mut? mut?)
{
	return ref(v->d2)@abi_chunk mut? mut?;
};

val_zero_as : func(to : ty, out : val mut? -> v0)
{
	i : u64 mut;
	out->type = to;
	if((ty_integral_size(to) != 0) || ty_is_enumeration(to))
	{
		out->tag = val_tag.scalar;
		irval_qimm(0, val_scalar_get_irval(out));
		return;
	}
	if(ty_is_pointer(to))
	{
		out->tag = val_tag.scalar;
		irval_qimm(0, val_scalar_get_irval(out));
		return;
	}
	if(ty_is_function(to))
	{
		out->tag = val_tag.scalar;
		irval_qimm(0, val_scalar_get_irval(out));
		return;
	}
	if(ty_is_bool(to))
	{
		out->tag = val_tag.scalar;
		irval_bimm(0, val_scalar_get_irval(out));
		return;
	}
	if(ty_is_array(to) || ty_is_structure(to))
	{
		out->tag = val_tag.aggregate;
		toty ::= to;
		members_count : u64 mut;
		abi_chunk_tys(abi_tag.psy64, ref toty, 1, val_aggregate_get_chunks(out), ref members_count, global_arena);
		members_ptr : val mut? := arena_alloc(global_arena, members_count * sizeof val);
		for(i = 0; i < members_count; i = i + 1)
		{
			val_zero_as(ty_at(to, i), members_ptr # i);
		}
		
		[val_aggregate_get_members_count(out)] = members_count;
		[val_aggregate_get_members(out)] = members_ptr;
		return;
	}
	panic_begin();
	putzstr("reached end of val_zero_as for type \0");
	putchar('"');
	putty(to);
	putchar('"');
	panic_end();
};

val_load_if_pointer : func(in : val mut?, out : val mut? -> v0)
{
	if(in->tag == val_tag.pointer)
	{
		val_force_load(in, out);
		return;
	}
	if(out != in)
	{
		[out] = [in];
	}
};

val_force_load : func(in : val mut?, out : val mut? -> v0)
{
	// we can load a pointer (expected) or even a scalar by pretending it was a pointer with no offset
	load_chunks : abi_chunk mut? mut;
	load_chunks_count : u64 mut;
	abi_chunk_tys(abi_tag.psy64, ref(in->type), 1, ref load_chunks, ref load_chunks_count, global_arena);

	// prepare the load(s)
	ptr : irval mut? mut;
	disp : s32 mut;

	if(in->tag == val_tag.pointer)
	{
		ptr = val_pointer_get_irval(in);
		disp = [val_pointer_get_displacement(in)];
	}
	else
	{
		ptr = val_scalar_get_irval(in);
		disp = 0;
	}

	// do one load per chunk
	// if there's only one chunk then we output a scalar
	if(load_chunks_count == 1)
	{
		ir_load(val_scalar_get_irval(out), abi_chunk_as_irsz(load_chunks # 0), ptr, disp);
		out->tag = val_tag.scalar;
		return;
	}
	// load all chunks at their expected offsets
	i : u64 mut;
	out_members : val mut? := arena_alloc(global_arena, load_chunks_count * sizeof val);
	for(i = 0; i < load_chunks_count; i = i + 1)
	{
		cur_out_member ::= out_members # i;
		cur_out_member->tag = val_tag.scalar;
		ir_load(val_scalar_get_irval(cur_out_member), abi_chunk_as_irsz(load_chunks # i), ptr, disp + (load_chunks # i)->src_byte_offset@s32);
	}
	out->tag = val_tag.aggregate;
	[val_aggregate_get_members(out)] = out_members;
	[val_aggregate_get_chunks(out)] = load_chunks;
	[val_aggregate_get_members_count(out)] = load_chunks_count;
};

type_from_node : func(ctx : source_context mut? -> ty)
{
	ctxcpy : source_context mut := [ctx];
	ctxcpy.type_only = true;
	v : val mut;
	val_from_node(ref ctxcpy, ref v);
	return v.type;
};

val_from_node : func(ctx : source_context mut?, out : val mut? -> v0)
{
	[val_from_node_tbl # (ctx->node->tag@s64)](ctx, out);
};

val_from_node_nyi : func(ctx : source_context mut?, out : val mut? -> v0)
{
	panic_begin();
	putzstr("val_from_node NYI for ast_tag.\0");
	putsint(ctx->node->tag@s64);
	putzstr(" at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_from_node_expr : func(ctx : source_context mut?, out : val mut? -> v0)
{
	[val_from_node_expr_tbl # ([ast_expr_get_tag(ctx->node)])@s64](ctx, out);
};

val_from_node_expr_nyi : func(ctx : source_context mut?, out : val mut?  -> v0)
{
	panic_begin();
	putzstr("val_from_node_expr NYI for ast_expr_tag.\0");
	putsint([ast_expr_get_tag(ctx->node)]@s64);
	putzstr(" at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_from_node_expr_symbol : func(ctx : source_context mut?, out : val mut? -> v0)
{
	sym ::= [ast_symbol_get_data(ctx->node)];
	len ::= [ast_symbol_get_data_len(ctx->node)];

	maybe_fn ::= prog_find_function(sym, len);
	if(maybe_fn != zero)
	{
		out->tag = val_tag.scalar;
		out->type = maybe_fn->type;
		irval_fnref(maybe_fn->irfunc_idx, val_scalar_get_irval(out));
		return;
	}
	panic_begin();
	putzstr("val_from_node_expr_symbol NYI at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_from_node_expr_literal : func(ctx : source_context mut?, out : val mut? -> v0)
{
	[val_from_node_expr_literal_tbl # [ast_literal_get_tag(ctx->node)]@s64](ctx, out);
};

val_from_node_expr_literal_integer : func(ctx : source_context mut?, out : val mut? -> v0)
{
	out->tag = val_tag.scalar;
	out->type = ty_integer_literal();
	// if we are type only do not do ir stuff.
	if(ctx->type_only)
	{
		return;
	}
	irval_qimm([ast_literal_get_integer(ctx->node)], val_scalar_get_irval(out));
};

val_from_node_expr_literal_floating_point : func(ctx : source_context mut?, out : val mut? -> v0)
{
	out->tag = val_tag.scalar;
	out->type = ty_floating_point_literal();
	// if we are type only do not do ir stuff.
	if(ctx->type_only)
	{
		return;
	}
	irval_gimm([ast_literal_get_floating_point(ctx->node)], val_scalar_get_irval(out));
};

val_from_node_expr_literal_char : func(ctx : source_context mut?, out : val mut? -> v0)
{
	out->tag = val_tag.scalar;
	out->type = ty_char_literal();
	// if we are type only do not do ir stuff.
	if(ctx->type_only)
	{
		return;
	}
	irval_bimm([ast_literal_get_char(ctx->node)], val_scalar_get_irval(out));
};

val_from_node_expr_literal_string : func(ctx : source_context mut?, out : val mut? -> v0)
{
	out->tag = val_tag.scalar;
	out->type = ty_string_literal();
	// if we are type only do not do ir stuff.
	if(ctx->type_only)
	{
		return;
	}
	ir_add_glob([ast_literal_get_string(ctx->node)], [ast_literal_get_string_len(ctx->node)], false, val_scalar_get_irval(out));
};

val_from_node_expr_literal_boolean : func(ctx : source_context mut?, out : val mut? -> v0)
{
	out->tag = val_tag.scalar;
	out->type = ty_boolean_literal();
	// if we are type only do not do ir stuff.
	if(ctx->type_only)
	{
		return;
	}
	irval_bimm([ast_literal_get_bool(ctx->node)], val_scalar_get_irval(out));
};

val_from_node_expr_zero_value : func(ctx : source_context mut?, out : val mut? -> v0)
{
	panic_begin();
	putzstr("val_from_node_expr_zero_value ought to be unreachable at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_from_node_expr_call : func(ctx : source_context mut?, out : val mut?  -> v0)
{
	// we expect callee scalar
	callee_node ::= ast_call_get_callee(ctx->node);

	callee_ctx : source_context mut := [ctx];
	callee_ctx.node = callee_node;

	callee_val : val mut;
	val_from_node(ref callee_ctx, ref callee_val);
	val_load_if_pointer(ref callee_val, ref callee_val);
	if(ctx->type_only)
	{
		out->type = callee_val.type;
		return;
	}
	panic_begin();
	putzstr("val_from_node_expr_call NYI at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_from_node_expr_blkinit : func(ctx : source_context mut?, out : val mut?  -> v0)
{
	out->tag = val_tag.aggregate;
	typename_ctx : source_context mut := [ctx];
	typename_ctx.node = ast_blkinit_get_typename(ctx->node);
	out->type = ty_from_typename_node(ref typename_ctx);
	if(out->type == ty.invalid)
	{
		err_begin();
		putzstr("block initialiser of unknown type at \0");
		putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
		err_end();
	}
	// if we are type only do not do ir stuff.
	if(ctx->type_only)
	{
		return;
	}
	members_count ::= ty_countof(out->type);
	chunks_count : u64 mut;
	// populate the output as an aggregate
	abi_chunk_tys(abi_tag.psy64, ty_unwrap(out->type)->members, ty_countof(out->type), val_aggregate_get_chunks(out), ref chunks_count, global_arena);
	[val_aggregate_get_members_count(out)] = chunks_count;
	chunk_vals : val mut? := arena_alloc(global_arena, chunks_count * sizeof typeof(val));
	[val_aggregate_get_members(out)] = chunk_vals;

	// the output is already ready, but chunk_vals is a pointer to its aggregate values, which we need to populate.
	// fill them all with val_tag.invalid for now and overwrite them later (that lets us check if the user did not initialise everything by checking for invalid-ness)
	i : u64 mut;
	for(i = 0; i < chunks_count; i = i + 1)
	{
		(chunk_vals # i)->tag = val_tag.invalid;
	}
	init_nodes ::= ast_blkinit_get_inits(ctx->node);
	inits_count ::= ast_blkinit_get_inits_count(ctx->node)@u64;
	member_idx : u64 mut;
	cur_chunk : abi_chunk mut? mut;

	for(i = 0; i < inits_count; i = i + 1)
	{
		init_node ::= init_nodes # i;
		// find out our member idx
		init_tag ::= [ast_init_get_tag(init_node)];
		if(init_tag == ast_init_tag.ellipsis)
		{
			panic_begin();
			putzstr("ellipsis syntax in initialisers is NYI at \0");
			putsrc(ctx->src, init_node->src_beg@u64, init_node->src_end@u64, putcol.red_underline);
			panic_end();
		}
		if(init_tag == ast_init_tag.integer)
		{
			member_idx = [ast_init_get_integer(init_node)]@u64;
		}
		if(init_tag == ast_init_tag.symbol)
		{
			// todo: assert out->type is a structure and not an array
			member_idx = ty_struct_find(out->type, [ast_init_get_symbol(init_node)], [ast_init_get_symbol_len(init_node)]);
		}
		if(member_idx >= ty_countof(out->type))
		{
			err_begin();
			putzstr("initialiser did not correspond to a valid member of the aggregate-type \0");
			putchar('"');
			putty(out->type);
			putchar('"');
			putzstr(" at \0");
			putsrc(ctx->src, init_node->src_beg@u64, init_node->src_end@u64, putcol.red_underline);
			err_end();
		}
		// our member value will correspond to one (scalar) or more (aggregate) members of chunk_vals
		// let's find the offset to the correct chunk(s) and set member_idx_as_chunk_idx to it
		member_idx_as_chunk_idx : u64 mut := ~0;
		j : u64 mut := 0;
		for(j = 0; j < chunks_count; j = j + 1)
		{
			cur_chunk = [val_aggregate_get_chunks(out)] # j;
			if(cur_chunk->src_idx_begin == member_idx)
			{
				member_idx_as_chunk_idx = j;
				j = chunks_count;
			}
		}
		if((chunk_vals # member_idx_as_chunk_idx)->tag != val_tag.invalid)
		{
			warn_begin();
			putzstr("chunk \0");
			putuint(member_idx_as_chunk_idx);
			putzstr(" within block-initialiser is initialised more than once at \0");
			putsrc(ctx->src, init_node->src_beg@u64, init_node->src_end@u64, putcol.yellow_underline);
			warn_end();
		}
		// finally get the val for the initialised member and convert it to the type
		cur_member_val : val mut;

		init_expr_ctx : source_context mut := [ctx];
		init_expr_ctx.node = ast_init_get_expr(init_node);
		if(ast_is_expr(init_expr_ctx.node, ast_expr_tag.zero_value))
		{
			// handle zero value
			val_zero_as(ty_at(out->type, member_idx), ref cur_member_val);
		}
		else
		{
			val_from_node(ref init_expr_ctx, ref cur_member_val);
			val_conv(ref cur_member_val, ty_at(out->type, member_idx), ref cur_member_val, ref init_expr_ctx);
		}
		// if we are a scalar we just write to chunk_vals[member_idx_as_chunk_idx]
		// if we are an aggregate then we siomply write to chunk_vals[member_idx_as_chunk_idx...val_aggregate_get_members_count]
		if(cur_member_val.tag == val_tag.scalar)
		{
			[chunk_vals # member_idx_as_chunk_idx] = cur_member_val;
		}
		if(cur_member_val.tag == val_tag.aggregate)
		{
			cur_agg_count ::= [val_aggregate_get_members_count(ref cur_member_val)];
			cur_agg_chunks ::= [val_aggregate_get_members(ref cur_member_val)];
			for(j = 0; j < cur_agg_count; j = j + 1)
			{
				[chunk_vals # (member_idx_as_chunk_idx + j)] = [cur_agg_chunks # j];
			}
		}
		if((cur_member_val.tag != val_tag.scalar) && (cur_member_val.tag != val_tag.aggregate))
		{
			panic_begin();
			putzstr("unexpected val_tag.\0");
			putsint(cur_member_val.tag@s64);
			putzstr(", expected scalar/aggregate at \0");
			putsrc(ctx->src, init_node->src_beg@u64, init_node->src_end@u64, putcol.red_underline);
			panic_end();
		}
	}

	// iterate again and error out if we didnt initialise any of the chunks
	for(i = 0; i < chunks_count; i = i + 1)
	{
		cur_chunk = [val_aggregate_get_chunks(out)] # i;
		if((chunk_vals # i)->tag == val_tag.invalid)
		{
			err_begin();
			putzstr("block initialiser failed to initialise member \0");
			putuint(cur_chunk->src_idx_begin);
			if(ty_is_structure(out->type))
			{
				struct_member_decl ::= ast_structure_get_data_members(ty_unwrap(out->type)->reference) # i;
				putzstr(" ('\0");
				putchars([ast_decl_get_name(struct_member_decl)], [ast_decl_get_name_len(struct_member_decl)]);
				putzstr("')");
			}
			putzstr(" at \0");
			putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
			err_end();
		}
	}
};

val_from_node_expr_field : func(ctx : source_context mut?, out : val mut? -> v0)
{
	// check if lhs is a symbol that matches an enum name
	lhs_node ::= ast_field_get_lhs(ctx->node);
	rhs_node ::= ast_field_get_rhs(ctx->node);
	is_ptr_field ::= [ast_field_get_parse_state(ctx->node)] == ast_field_parse_state.finalisedarrow;
	if(ast_is_expr(lhs_node, ast_expr_tag.symbol) && (is_ptr_field == false))
	{
		lhs_as_type ::= ty_find_by_name([ast_symbol_get_data(lhs_node)], [ast_symbol_get_data_len(lhs_node)]);
		if(lhs_as_type != ty.invalid)
		{
			if(ty_is_enumeration(lhs_as_type))
			{
				val_from_node_expr_enum_field(lhs_as_type, rhs_node, ctx, out);
				return;
			}
		}
	}
	panic_begin();
	putzstr("todo: val_from_node_expr_field at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_from_node_expr_enum_field : func(type : ty, rhs_symbol_node : ast mut?, ctx : source_context mut?, out : val mut? -> v0)
{
	// we are dealing with a field expression, but the lhs was a symbol that corresponded to an enum type (provided here via the 'type' parameter)
	// in this case, we assume it is not a struct-access and assert that the rhs is a symbol corresponding to an enum entry
	// if it is, 'out' becomes a scalar immediate corresponding to the entry.
	// if it isn't, error out.
	// if we are type only do not do ir stuff.
	if(ctx->type_only)
	{
		out->type = type;
		return;
	}
	if(ast_is_expr(rhs_symbol_node, ast_expr_tag.symbol) == false)
	{
		err_begin();
		putzstr("lhs of field-expression was enum type \0");
		putchar('"');
		putty(type);
		putchar('"');
		putzstr(", but the rhs was not a symbol expression. it should be a symbol corresponding to an entry name of the enum type at \0");
		putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
		err_end();
	}
	entry_value ::= ty_enum_find(type, [ast_symbol_get_data(rhs_symbol_node)], [ast_symbol_get_data_len(rhs_symbol_node)]);
	if(entry_value == zero)
	{
		err_begin();
		putzstr("enum type \0");
		putchar('"');
		putty(type);
		putchar('"');
		putzstr(" had no entry named \0");
		putchar('"');
		putchars([ast_symbol_get_data(rhs_symbol_node)], [ast_symbol_get_data_len(rhs_symbol_node)]);
		putchar('"');
		putzstr(" at \0");
		putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
		err_end();
	}
	out->type = type;
	out->tag = val_tag.scalar;
	irval_qimm([entry_value], val_scalar_get_irval(out));
};

val_from_node_expr_countof : func(ctx : source_context mut?, out : val mut? -> v0)
{
	countof_typename_ctx : source_context mut := [ctx];
	countof_typename_ctx.node = ast_countof_get_operand(ctx->node);
	type ::= ty_from_typename_node(ref countof_typename_ctx);
	// if we are type only do not do ir stuff.

	out->type = ty_integer_literal();
	out->tag = val_tag.scalar;
	if(ctx->type_only)
	{
		return;
	}
	irval_qimm(ty_countof(type), val_scalar_get_irval(out));
};

val_from_node_stmt : func(ctx : source_context mut?, out : val mut? -> v0)
{
	[val_from_node_stmt_tbl # ([ast_stmt_get_tag(ctx->node)]@s64)](ctx, out);
};

val_from_node_stmt_nyi : func(ctx : source_context mut?, out : val mut? -> v0)
{
	panic_begin();
	putzstr("val_from_node_stmt NYI for ast_stmt_tag.\0");
	putsint([ast_stmt_get_tag(ctx->node)]@s64);
	putzstr(" at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_conv : func(in : val mut?, to : ty, out : val mut?, ctx : source_context mut? -> v0)
{
	conv ::= ty_converts_to(in->type, to);
	if(tyconv_is_valid(conv))
	{
		if(ctx->type_only)
		{
			out->type = to;
			return;
		}
		val_load_if_pointer(in, in);
		[val_conv_tbl # (in->tag@s64)](in, to, out, conv, ctx);
		return;
	}
	err_begin();
	putzstr("cannot convert value of type \0");
	putchar('"');
	putty(in->type);
	putchar('"');
	putzstr(" to type \0");
	putchar('"');
	putty(to);
	putchar('"');
	if(conv == tyconv.no_cant_add_staticness)
	{
		putzstr(" (because doing so would add staticness)\0");
	}
	if(conv == tyconv.no_but_yes_with_weak)
	{
		putzstr(" (but could if one were marked 'weak')\0");
	}
	putzstr(" at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	err_end();
};

val_conv_invalid : func(in : val mut?, to : ty, out : val mut?, conv : tyconv, ctx : source_context mut? -> v0)
{
	panic_begin();
	putzstr("reached val_conv_invalid at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_conv_scalar : func(in : val mut?, to : ty, out : val mut?, conv : tyconv, ctx : source_context mut? -> v0)
{
	[val_conv_scalar_tbl # (conv@s64)](in, to, out, conv, ctx);
};

val_conv_scalar_nyi : func(in : val mut?, to : ty, out : val mut?, conv : tyconv, ctx : source_context mut? -> v0)
{
	panic_begin();
	putzstr("reached val_conv_scalar_nyi for tyconv.\0");
	putsint(conv@s64);
	putzstr(" at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_conv_scalar_noop : func(in : val mut?, to : ty, out : val mut?, conv : tyconv, ctx : source_context mut? -> v0)
{
	// noop
	[out] = [in];
	out->type = to;
};

val_conv_pointer : func(in : val mut?, to : ty, out : val mut?, conv : tyconv, ctx : source_context mut? -> v0)
{
	panic_begin();
	putzstr("todo: val_conv_pointer at \0");
	putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
	panic_end();
};

val_conv_aggregate : func(in : val mut?, to : ty, out : val mut?, conv : tyconv, ctx : source_context mut? -> v0)
{
	if(ty_strip(in->type) != ty_strip(to))
	{
		panic_begin();
		putzstr("requested to convert aggregate value of type \0");
		putty(in->type);
		putzstr(" to dissimilar type \0");
		putty(to);
		putzstr(" - this should've been caught and error'd properly earlier at \0");
		putsrc(ctx->src, ctx->node->src_beg@u64, ctx->node->src_end@u64, putcol.red_underline);
		panic_end();
	}
	// noop
	[out] = [in];
	out->type = to;
};

val_from_node_tbl ::= typeof(val_from_node)[countof ast_tag]
{
	.0 := val_from_node_nyi;
	.1 := val_from_node_nyi;
	.2 := val_from_node_expr;
	.3 := val_from_node_stmt;
	.4 := val_from_node_nyi;
	.5 := val_from_node_nyi;
	.6 := val_from_node_nyi;
	.7 := val_from_node_nyi;
	.8 := val_from_node_nyi;
	.9 := val_from_node_nyi;
	.10 := val_from_node_nyi;
	.11 := val_from_node_nyi;
	.12 := val_from_node_nyi;
	.13 := val_from_node_nyi;
	.14 := val_from_node_nyi;
	.15 := val_from_node_nyi;
	.16 := val_from_node_nyi;
	.17 := val_from_node_nyi;
	.18 := val_from_node_nyi;
	.19 := val_from_node_nyi;
};

val_from_node_expr_tbl ::= typeof(val_from_node)[countof ast_expr_tag]
{
	.0 := val_from_node_expr_symbol;
	.1 := val_from_node_expr_literal;
	.2 := val_from_node_expr_zero_value;
	//.3 := val_from_node_expr_unop;
	.3 := val_from_node_expr_nyi;
	//.4 := val_from_node_expr_biop;
	.4 := val_from_node_expr_nyi;
	.5 := val_from_node_expr_call;
	.6 := val_from_node_expr_blkinit;
	//.7 := val_from_node_expr_dereference;
	.7 := val_from_node_expr_nyi;
	//.8 := val_from_node_expr_cast;
	.8 := val_from_node_expr_nyi;
	.9 := val_from_node_expr_field;
	//.10 := val_from_node_expr_sizeof;
	.10 := val_from_node_expr_nyi;
	//.11 := val_from_node_expr_alignof;
	.11 := val_from_node_expr_nyi;
	.12 := val_from_node_expr_countof;
	//.13 := val_from_node_expr_typeof;
	.13 := val_from_node_expr_nyi;
};

val_from_node_expr_literal_tbl ::= typeof(val_from_node)[countof ast_literal_tag]
{
	.0 := val_from_node_expr_literal_integer;
	.1 := val_from_node_expr_literal_floating_point;
	.2 := val_from_node_expr_literal_char;
	.3 := val_from_node_expr_literal_string;
	.4 := val_from_node_expr_literal_boolean;
};

val_from_node_stmt_tbl ::= typeof(val_from_node)[countof ast_expr_tag]
{
	.0 := val_from_node_expr;
	//.1 := val_from_node_stmt_decl;
	.1 := val_from_node_stmt_nyi;
	.2 := val_from_node_stmt_nyi;
	.3 := val_from_node_stmt_nyi;
	.4 := val_from_node_stmt_nyi;
	.5 := val_from_node_stmt_nyi;
	.6 := val_from_node_stmt_nyi;
	//.7 := val_from_node_stmt_if_blk;
	.7 := val_from_node_stmt_nyi;
	.8 := val_from_node_stmt_nyi;
	//.9 := val_from_node_stmt_while_blk;
	.9 := val_from_node_stmt_nyi;
	//.10 := val_from_node_stmt_for_blk;
	.10 := val_from_node_stmt_nyi;
	//.11 := val_from_node_stmt_return;
	.11 := val_from_node_stmt_nyi;
	//.12 := val_from_node_stmt_unreachable;
	.12 := val_from_node_stmt_nyi;
	.13 := val_from_node_stmt_nyi;
};

val_conv_tbl ::= typeof(val_conv_invalid)[countof val_tag]
{
	.0 := val_conv_invalid;
	.1 := val_conv_scalar;
	.2 := val_conv_pointer;
	.3 := val_conv_aggregate;
};

val_conv_scalar_tbl ::= typeof(val_conv_invalid)[countof tyconv]
{
	.0 := val_conv_scalar_noop;
	.1 := val_conv_scalar_noop;
	.2 := val_conv_scalar_noop;
	.3 := val_conv_scalar_nyi;
	.4 := val_conv_scalar_nyi;
	.5 := val_conv_scalar_nyi;
	.6 := val_conv_scalar_nyi;
	.7 := val_conv_scalar_noop;
	.8 := val_conv_scalar_nyi;
	.9 := val_conv_scalar_nyi;
	.10 := val_conv_scalar_nyi;
	.11 := val_conv_scalar_nyi;
	.12 := val_conv_scalar_nyi;
	.13 := val_conv_scalar_nyi;
	.14 := val_conv_scalar_nyi;
	.15 := val_conv_scalar_noop;
	.16 := val_conv_scalar_nyi;
	.17 := val_conv_scalar_nyi;
	.18 := val_conv_scalar_nyi;
};
