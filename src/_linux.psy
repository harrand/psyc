static if(_linux)
{
	timespec ::= struct
	{
		tv_sec : s64;
		tv_nsec : s64;
	};

	clock_gettime ::= func(clk_id : s32, tp : timespec mut? -> s32) := extern;
	getcwd ::= func(buf : u8?, size : u64 -> u8?) := extern;
	chdir ::= func(path : u8? -> s32) := extern;
}
