// e
xd ::= 5;

shooey ::= func(gee : u64, maga : myhardstruct mut weak static) -> mycomplexstruct
{
	//return zero;
};

mycomplexstruct ::= struct
{
	easymem : s64;
	hardmem : mystruct;
	hardestmem : myhardstruct mut& mut weak;
	//evilmem : mycomplexstruct mut weak&;
};

myhardstruct ::= struct
{
	fooey : u64;
};
